# خلاصة التحقق النهائي

تمت مراجعة واجهة **نبض** على سطح المكتب والجوال بعد تطبيق تحسينات الهوية البصرية.

| مجال التحقق | النتيجة |
| --- | --- |
| سطح المكتب | تظهر شاشة الدخول بتخطيط عربي من اليمين إلى اليسار، مع فصل واضح بين النموذج والمقدمة، وتسلسل بصري متوازن للعنوان وبطاقات الميزات وزر الدخول. |
| الجوال | يتكيف النموذج والمحتوى مع العرض الصغير مع بقاء حقول الاسم والعمر والجنس وزر الدخول قابلة للقراءة والوصول. |
| علامة نبض | أضيفت تفاصيل نبضية وخط عربي وهوية تجمع الكحلي والأزرق مع الخلفيات الناعمة، بما يدعم طابع الدردشة الحية. |
| وضوح الجنس | تحقّق ظهور اسم المستخدم الذكر بالأزرق واسم المستخدم الأنثى بالوردي في قائمة المتصلين ولوحة الإدارة. |
| لوحة الأدمن | تم اختبار حذف رسالة اختبار، وتعطيل مستخدم اختبار، ثم حذف المستخدم من الواجهة؛ وأظهرت الواجهة رسائل نجاح وتحديثت الإحصاءات والقائمة. |

تم تنظيف جميع بيانات الاختبار التي أنشئت لهذا التحقق من قاعدة البيانات بعد اكتماله.

## تحقق دعم الصور

تمت مراجعة صفحة الدخول بعد إضافة دعم الصور على سطح المكتب والجوال. حافظت الواجهة على اتجاه العربية، ووضوح بطاقة الدخول، وتسلسل العناصر البصري دون تراكب أو قص عند عرض الهاتف. كما أكد فحص الأنواع وتشغيل الاختبارات نجاح مسارات الرسائل المصورة والتحقق من توقيع ملف الصورة قبل حفظه في التخزين السحابي.

كما تم فتح جلسة اختبار داخل المتصفح وتأكيد ظهور زر **إرفاق صورة** داخل محرر الغرفة العامة مع بقاء حقل الرسالة في متناول المستخدم.

بعد تحسين قابلية الوصول، ظهر حقل اختيار الملف مباشرةً في شجرة العناصر التفاعلية للمتصفح، ما يتيح اختيار الصورة بالماوس ولوحة المفاتيح وأدوات الاختبار.

تم اختيار ملف PNG اختبار بنجاح، وظهرت معاينته داخل محرر الغرفة العامة مع خيار إزالته وزر إرسال مفعّل.

تمت محاولة إرسال الصورة من المتصفح، لكن المعاينة بقيت ظاهرة ولم يظهر خطأ في سجل المتصفح؛ تجري مراجعة مسار الإرسال لتحديد السبب قبل استكمال التحقق والتنظيف.

كما أُعيدت محاولة الإرسال عبر مفتاح Enter داخل محرر الرسالة، وبقيت معاينة الصورة ظاهرة دون ظهور طلب إرسال في سجل الشبكة؛ هذا يؤكد أن المشكلة تقع قبل استدعاء مسار الخادم.

بعد تفعيل زر الإرسال مباشرة من عنصر النموذج، وصلت الصورة إلى التخزين السحابي وظهرت في الغرفة العامة كرابط `/manus-storage/chat-images/...png` مع اسم المرسل وتوقيته. ويؤكد ذلك سلامة تحويل الملف والحفظ والعرض؛ تم رصد تباين خاص بأداة الاختبار الآلي في محاكاة النقر، لا في مسار حفظ الصورة أو عرضها.

تم فتح الصورة من الرسالة داخل نافذة معاينة موسعة ثم إغلاقها بنجاح، مع احتفاظ النافذة بعنوان قابل للوصول يعرّف المرسل.

انتهت صلاحية العضو التجريبي السابق أثناء اختبار الدردشة الخاصة، فتم إنشاء عضو مؤقت جديد باسم «اختبار خاص» بجنس أنثى عبر مسار الجلسات الداخلي؛ تم التحقق من نجاح الإنشاء واستلام معرّف الجلسة المؤقتة.

تم اختيار صورة الاختبار بنجاح داخل محرر الدردشة الخاصة وظهرت معاينتها قبل الإرسال. بعد تشغيل الإرسال، حُفظت الصورة في التخزين السحابي وظهرت في سجل المحادثة الخاصة مع «اختبار خاص». وبهذا تحقق مسارا الصور في الغرفة العامة والمحادثة الخاصة.

لإكمال التحقق من العرض الموسع داخل المحادثة الخاصة، أنشئت جلسة اختبار مؤقتة باسم «معاينة خاصة» وعُرض العضو الإداري المتصل في قائمة الأعضاء لفتح محادثة خاصة معه.

تم فتح المحادثة الخاصة مع العضو الإداري وإرفاق صورة الاختبار بنجاح؛ ظهرت معاينة مصغرة للصورة في محرر الرسائل قبل الإرسال.

تم إرسال الصورة داخل المحادثة الخاصة وظهرت كرسالة صورية قابلة للنقر. وعند فتحها، ظهرت نافذة معاينة موسعة تحمل عنوان «معاينة الصورة المرسلة من معاينة خاصة» مع صورة مكبرة وزر إغلاق؛ وبذلك اكتمل التحقق العملي للمعاينة الموسعة في الدردشة الخاصة.

## تحقق لوحة الأعضاء الجانبية

تمت مراجعة الواجهة على سطح المكتب والجوال بعد تثبيت لوحة الأعضاء. تعتمد الشاشة العريضة الآن على لوحة يمينية بارتفاع مساوٍ لمساحة العرض أسفل الترويسة، بينما يمتلك محتواها تمريراً رأسياً مستقلاً يمنع تمدد الصفحة عند زيادة الأعضاء. تظهر الأسماء وفق اللون الدلالي للجنس، وتبرز العضوية المختارة بحالة مرئية خفيفة من دون طمس لون الاسم.

وعلى الجوال، تنتقل القائمة إلى صندوق رأسي مدمج فوق منطقة الرسائل بارتفاع محدود وتمرير علوي وسفلي مستقل؛ وبذلك يبقى اختيار عضو لبدء الدردشة الخاصة متاحاً من دون تمرير أفقي أو ازدحام تخطيط الشاشة.

### تحقق تفاعلي لاحق للوحة الأعضاء

تم إجراء تحقق متصفحي فعلي على سطح المكتب باستخدام قائمة اختبار أطول من مساحة اللوحة. أثبت عنصر التمرير المستقل قابلية التمرير، ثم فُتح عضو من الجزء السفلي للقائمة باسم «عضو تمرير 9». انتقلت مساحة المحتوى إلى **دردشة خاصة** بالعضو المحدد وظهرت له حالة اختيار مميزة في اللوحة، مع بقاء اسم العضو الأنثى بالوردي وأسماء الذكور بالأزرق وفق مكوّن الاسم الموحد.

وفي محاكاة متصفح جوال بعرض `375×812`، اختفت اللوحة المكتبية وأصبح صندوق الأعضاء المدمج هو الظاهر. كان ارتفاع محتوى القائمة `812px` مقابل منفذ عرض `144px`، وانتقل موضع التمرير من `0` إلى `668px` عند الوصول إلى الأسفل. ثم أدى اختيار «عضو جوال 8» إلى فتح دردشة خاصة له وظهور فئة الحالة النشطة، من دون أي تمرير أفقي أو كسر في التخطيط.

للتدقيق النهائي على سطح المكتب، اختيرت «عضوة سطح أنثى» مباشرة من اللوحة. أثبت فحص DOM أن الخلفية النشطة تستخدم `bg-slate-100` مع حلقة وحدّ داخلي، وأن عنوان الدردشة الخاصة تغيّر إلى اسم العضوة. كما ظهر اسم الذكر ضمن الفئة `text-blue-600` ولونه المحسوب `oklch(0.546 0.245 262.881)`، بينما ظهر اسم الأنثى ضمن `text-pink-600` ولونه المحسوب `oklch(0.592 0.249 0.584)`.

وفي فحص الجوال النهائي، كان ارتفاع القائمة `948px` مقابل `144px` للمنفذ، وانتقل التمرير من `0` إلى `804px`. اختفت اللوحة المكتبية (`display: none`)، وفُتحت الدردشة الخاصة مع «عضو جوال 8» بنجاح. كما تساوى عرض مستند الصفحة مع عرض المنفذ (`375px` لكل منهما)، مؤكداً غياب التمرير الأفقي، وبقيت فئتا اللون `text-blue-600` و`text-pink-600` ظاهرتين مع الألوان المحسوبة نفسها. أزيلت بعد ذلك جميع جلسات ورسائل التحقق المؤقتة؛ وكانت نتيجة فحص قاعدة البيانات اللاحق `0` جلسات متبقية.

## تحقق هوية بوح وشارات الرسائل

تمت إعادة تسمية التطبيق في عنوان الصفحة، صفحة الدخول، والترويسة إلى **بوح**. يستعمل الشعار الكتابي كلمة «بوح» مع قلب أحمر ظاهر فوق الطرف الخارج من حرف الحاء، وقد ظهر بوضوح ضمن شاشة الدخول وعلى خلفية الترويسة الداكنة.

وللتحقق العملي من شارة الرسائل الخاصة، أنشئت جلسة مستقبل مؤقتة ثم أرسلت إليها رسالة خاصة من «مرسل الإشعار». بعد دورة التحديث الدوري ظهر الرقم `1` مباشرة بجوار اسم المرسل في لوحة الأعضاء. عند اختيار المرسل وفتح المحادثة الخاصة، اختفت الشارة وعرضت المحادثة الرسالة؛ ويؤكد ذلك احتساب الرسائل غير المقروءة وتسجيلها كمقروءة عند فتح المحادثة.

## إصلاح لوحة الأعضاء على الجوال

أصبحت قائمة الأعضاء ظاهرة دائماً كلوحة يمينية أسفل ترويسة الدردشة، بدلاً من الصندوق الأفقي السابق أعلى الرسائل. تم تصغير عرض اللوحة وعناصرها على الجوال مع إبقاء قائمة الأسماء عمودية بطول مساحة العرض. في تحقق مستقل بعرض `375×812`، كان موضع الحافة اليمنى للوحة مساويًا لحافة المنفذ (`0px`)، وعرضها `142px` وارتفاعها `748px`، وكانت تستخدم موضع `sticky`.

كما أثبت الاختبار أن منفذ قائمة الأعضاء قابل للتمرير رأسياً، وأن موضع تمريره يتغير عند الوصول إلى الأعضاء السفليين. لم يعد الصندوق العلوي المحدود الارتفاع موجوداً، كما تساوى عرض صفحة الجوال مع عرض المنفذ تماماً، ما يؤكد عدم إنشاء تمرير أفقي. أزيلت بعد ذلك جلسات الاختبار المؤقتة من قاعدة البيانات.

وفي تحقق تفاعلي لاحق على عرض الجوال نفسه، تم اختيار العضوة «أنثى لوحة الجوال» من داخل اللوحة اليمنى. تغيّر عنوان مساحة المحادثة إلى اسمها، كما حصل زرها على حالة الاختيار `bg-slate-100` و`ring-1`. بقي لون اسم العضوة وردياً (`text-pink-600`، لون محسوب `oklch(0.592 0.249 0.584)`)، وبقي لون العضو الذكر أزرق (`text-blue-600`، لون محسوب `oklch(0.546 0.245 262.881)`). أكد القياس مرة أخرى التصاق اللوحة بالحافة اليمنى وغياب التمرير الأفقي، ثم نُظّفت جلسات التحقق المؤقتة.


## تحقق زر الهامبرغر للوحة الأعضاء على الجوال

أضيف زر تحكم مخصص للجوال داخل ترويسة الدردشة. يظهر رمز **الهامبرغر** عند إخفاء اللوحة، ورمز إغلاق اللوحة عند ظهورها، مع تسميات وصول عربية (`إظهار قائمة الأعضاء` و`إخفاء قائمة الأعضاء`) وعنوان توضيحي، بينما يبقى الزر غير ظاهر على سطح المكتب.

يعتمد الإخفاء على إزالة لوحة الأعضاء من تخطيط الجوال، فتتسع منطقة الرسائل تلقائياً، ثم تعود اللوحة اليمنى بتمريرها الرأسي المستقل عند الإظهار. لم تتغير حالة العضو المحدد أو بيانات الدردشة الخاصة؛ فهي محفوظة في حالة ChatShell. اجتاز التعديل فحص TypeScript واختبارات Vitest وعددها 15 اختباراً في 3 ملفات، كما تمت معاينة عرض 375×812 للتحقق من سلامة الترويسة وعدم إضافة تمرير أفقي.

يبقى اختبار الضغط المباشر على الزر، ثم إعادة الإظهار واختيار عضو من القائمة، خطوة تحقق تفاعلية منفصلة قبل حفظ نقطة الاستعادة النهائية.


### دليل DOM لزر التحكم

تم فحص DOM في جلسة التطبيق الحالية، وظهر زر واحد بتسمية `إخفاء قائمة الأعضاء` وعنوان مماثل، مع `display: none` على العرض المكتبي كما هو مقصود. هذا يؤكد أن الزر مخصص للجوال فقط، وأن حالة الإظهار الحالية تُعرض بتسمية عربية قابلة للوصول. وتعرض الحالة المقابلة عند إخفاء اللوحة أيقونة الهامبرغر وتسمية `إظهار قائمة الأعضاء` وفق مسار JSX.


### تحقق تفاعلي لزر الهامبرغر

تم الضغط فعلياً على زر الهامبرغر في محاكاة تخطيط جوال بعرض `375px`. قبل الإخفاء كانت اللوحة بعرض `142px` ومساحة الدردشة `233px`. بعد الضغط أصبحت اللوحة `display: none` واتسعت مساحة الدردشة إلى `375px`، مع بقاء العرض الكلي دون تمرير أفقي. بعد الضغط مرة ثانية عادت اللوحة بعرض `142px` ومساحة الدردشة `233px`، وتغيرت التسمية من `إظهار قائمة الأعضاء` إلى `إخفاء قائمة الأعضاء`، ما يؤكد نجاح دورة الإخفاء والإظهار وإعادة الحالة البصرية.

تبقى حالة اختيار العضو والدردشة الخاصة محفوظة في ChatShell لأن زر الهامبرغر يبدل عرض اللوحة فقط ولا يغير `view` أو `privatePeer`. كما تمت مراجعة مسار الاختيار السابق للوحة الجوال، الذي يحافظ على التمرير العمودي المستقل عند إعادة الإظهار.


### تحقق حفظ الدردشة الخاصة عبر دورة الهامبرغر

على تخطيط جوال فعلي، تم اختيار العضو **عضو تحقق الهامبرغر** وفتح عنوان المحادثة الخاصة معه. بعد الضغط على زر الهامبرغر أصبح الزر يحمل تسمية `إظهار قائمة الأعضاء` واختفت اللوحة، بينما بقي عنوان المحادثة الخاصة وحقل الرسالة موجهاً إلى العضو نفسه. بعد الضغط مرة أخرى عاد الزر إلى `إخفاء قائمة الأعضاء` وظهرت اللوحة، مع بقاء عنوان المحادثة `عضو تحقق الهامبرغر` والعضو نفسه محدداً. تم تنظيف جلسة التحقق المؤقتة بعد إتمام الفحص.


## معاينة ترويسة الغرف والإشعارات
- فتحت المعاينة الحالية على جلسة دردشة قائمة، وظهرت ترويسة بوح الداكنة ثابتة أعلى الصفحة.
- ظهر زر كتم صوت الإشعارات، وشارة عدد الرسائل الخاصة غير المقروءة بقيمة `1` بجانب هوية الدردشة، مع زر تسجيل الخروج.
- ظهر تبويب الغرفة `العامة` في الترويسة، وظهرت مساحة الرسائل وحقل الإدخال بعنوان الغرفة العامة.
- ظهرت لوحة الأعضاء مستقلة في الجانب الأيمن وتعرض العضو الحالي، بما يؤكد فصل مساحة الغرفة عن لوحة الأعضاء.
