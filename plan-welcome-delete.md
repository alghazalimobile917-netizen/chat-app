# خطة تطوير بوح: قائمة الجوال ورسائل الترحيب وحذف الرسائل

## الهدف
تحديث تطبيق الدردشة العربي «بوح» بحيث تبدأ لوحة الأعضاء **مخفية تلقائياً على الجوال فقط**، مع إبقائها ظاهرة على الويب، وإضافة رسالة ترحيب تلقائية عند دخول عضو جديد إلى الغرفة العامة باسمه، ومنح المسؤول صلاحية حذف أي رسالة في الدردشة العامة أو الخاصة.

## القرارات والافتراضات

| الموضوع | القرار المعتمد |
|---|---|
| لوحة الأعضاء | الحالة الابتدائية `false` على الشاشات الصغيرة فقط؛ لا يتغير سلوك سطح المكتب. يظل زر الهامبرغر متاحاً لإظهار اللوحة وإخفائها. |
| رسالة الترحيب | تُنشأ مرة واحدة عند دخول جلسة عضو جديدة إلى الغرفة العامة، وتظهر كرسالة نظام باسم العضو، لا كرسالة قابلة للرد. سيتم منع التكرار أثناء polling أو إعادة التصيير. |
| حذف الرسائل | المسؤول يستطيع حذف أي رسالة عامة أو خاصة من لوحة الإدارة، مع تأكيد قبل الحذف وتحديث فوري للقائمة والدردشة. لا يُمنح العضو العادي صلاحية الحذف وفق الطلب الحالي. |
| نطاق الترحيب | الغرفة العامة فقط؛ لا تُضاف رسائل ترحيب تلقائية عند دخول الغرف المستقلة أو فتح محادثة خاصة. |
| البيانات القديمة | لا تُعاد معالجة الرسائل القديمة؛ الترحيب يطبق على الجلسات الجديدة بعد نشر التعديل. |

## مراحل التنفيذ

### 1. فحص البنية الحالية
مراجعة `Home.tsx` ومنطق حالة لوحة الأعضاء والهامبرغر، ومسارات الجلسات والرسائل في `server/routers/chat.ts` و`server/db.ts`، ومخطط الرسائل في `drizzle/schema.ts`. تحديد أفضل نقطة لإنشاء حدث الدخول دون أن يتكرر أثناء polling.

### 2. تنفيذ الحالة الابتدائية للجوال
تعديل تهيئة حالة لوحة الأعضاء باستخدام hook الاستجابة الحالي أو media query موثوق، بحيث تكون مخفية عند بداية جلسة الجوال فقط. الحفاظ على حالة المستخدم بعد الضغط على الهامبرغر، وعدم تغيير تخطيط الويب أو التمرير الرأسي الحالي. إضافة حالة وصولية واضحة للزر مثل `aria-expanded` واسم يبين إظهار القائمة أو إخفاءها.

### 3. إضافة رسائل الترحيب العامة
توسيع نموذج الرسائل بتمييز رسائل النظام أو الترحيب، أو استخدام نوع رسالة واضح لا يخلطها برسائل الأعضاء. عند إنشاء/تجديد جلسة عضو جديد في الغرفة العامة، ينشئ الخادم رسالة ترحيب آمنة تتضمن الاسم الظاهر بعد تطهيره من المحتوى غير المرغوب. ضمان idempotency عبر معرف الجلسة أو قيد منطقي حتى لا يكررها polling، وتحديد كيفية عرضها بصرياً كرسالة نظام غير قابلة للإرسال كعضو.

### 4. تفعيل حذف المسؤول للرسائل العامة والخاصة
إضافة مسار tRPC محمي بصلاحية المسؤول لحذف رسالة بواسطة معرفها، مع التحقق من وجودها ومن نوعها، وتنظيف مرفق الصورة المرتبط بها وفق آلية التخزين الحالية إن كانت الرسالة مصورة. تحديث دوال قاعدة البيانات بحيث يشمل الحذف رسائل الغرفة العامة والرسائل الخاصة، ثم ربط الإجراء بلوحة الإدارة مع حوار تأكيد، وحالات تحميل/خطأ، وتحديث cache للدردشة ولوحة الإدارة بعد النجاح.

### 5. الاختبارات
إضافة اختبارات Vitest تشمل إنشاء رسالة ترحيب واحدة فقط عند دخول جلسة جديدة، عدم تكرارها عند إعادة الجلب، عزل الترحيب عن الغرف غير العامة، وصلاحية المسؤول في حذف الرسائل العامة والخاصة ومنع غير المسؤول. اختبار أن حذف رسالة مصورة لا يترك بيانات وصفية غير مرتبطة، وأن المسار يتعامل مع معرف رسالة غير موجود بأمان. إضافة تحقق واجهي لحالة لوحة الجوال وإعادة إظهارها بالهامبرغر.

### 6. التحقق المتصفحي والتسليم
اختبار على عرض جوال: الدخول بعضو جديد، التأكد من اختفاء لوحة الأعضاء أولاً، إظهارها وإخفائها بالهامبرغر، وملاحظة رسالة الترحيب باسمه في العامة. الدخول كمسؤول، حذف رسالة عامة ثم رسالة خاصة من لوحة الإدارة، والتأكد من اختفائهما بعد التحديث. تشغيل `pnpm check` و`pnpm test`، مراجعة حالة الخادم، ثم حفظ Checkpoint جديد وإرفاقه بالتسليم.

## المخاطر ونقاط الانتباه

قد يؤدي إنشاء رسالة الترحيب داخل مسار تجديد الجلسة إلى تكرار الرسائل إذا لم يُستخدم معرف جلسة أو قيد idempotency؛ لذلك ستكون هذه نقطة اختبار أساسية. حذف الرسائل المصورة يحتاج الحفاظ على اتساق قاعدة البيانات والتخزين، مع اعتبار حذف سجل الرسالة هو السلوك الأساسي الآمن إذا تعذر حذف الكائن السحابي فوراً. كما يجب عدم جعل حالة إخفاء الجوال تؤثر في سطح المكتب أو في اختيار العضو الحالي والدردشة الخاصة.

## مخرجات التسليم

سيتم تسليم إصدار منشور يتضمن لوحة أعضاء تبدأ مخفية على الجوال فقط، رسائل ترحيب عامة غير مكررة للأعضاء الجدد، حذفاً إدارياً للرسائل العامة والخاصة، اختبارات محدثة، ونقطة استعادة قابلة للرجوع.
